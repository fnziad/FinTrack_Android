# TakaKoi currently has no runtime secrets or network services.
# Keep this file empty unless a future feature adds a documented secret.
# Never commit a real key here; use local environment variables or protected
# GitHub/App Store release settings instead.
